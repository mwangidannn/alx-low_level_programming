#include "main.h"
/**
 * _abs - calcurate absolute value of integer
 * @b:  int to find
 * Return: computed absolute value of integer
 */
int _abs(int b)
{
	if (b >= 0)
	{
		return (b);
	}
	return (-b);
}
