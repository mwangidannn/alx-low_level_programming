#include "main.h"
/**
 * _isdigit - will checks for any digit (0 to 9)
 * @c: integer to checked if
 * Return: 1 if c is a interger digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

