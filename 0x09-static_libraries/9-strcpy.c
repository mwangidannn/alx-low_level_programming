#include "main.h"
/**
 * char *_strcpy - a function that copies the string pointed to by sour
 * @desti: copy to
 * @sour:copy from
 * Return: string
 */
char *_strcpy(char *desti, char *sour)
{
	int l = 0;
	int x = 0;


	while (*(sour + l) != '\0'
	{
		l++;
	}
	for ( ; x < l ; x++)
	{
		desti[x] = src[x];
	}
	desti[l] = '\0';
	return (desti);
}
